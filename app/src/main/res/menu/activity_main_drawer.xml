<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	tools:showIn="navigation_view">
	<group android:checkableBehavior="single">
		<item
			android:id="@+id/nav_home"
			android:icon="@drawable/ic_home"
			android:title="@string/dashboard" />
		<item
			android:id="@+id/nav_doc"
			android:icon="@drawable/ic_doc"
			android:title="@string/api_queue" />
		<item
			android:id="@+id/nav_plugins"
			android:icon="@android:drawable/btn_star_big_on"
			android:title="@string/retry_center" />
		<item
			android:id="@+id/nav_psg"
			android:icon="@drawable/ic_search"
			android:title="@string/monitoring" />
		<item
			android:id="@+id/nav_settings"
			android:icon="@drawable/ic_menu_manage"
			android:title="@string/settings" />
		<item
			android:id="@+id/nav_support"
			android:icon="@drawable/ic_help"
			android:title="@string/about_awy" />
	</group>

	<!--
	 Temporarily disabled theme toggle due to state-loss issues on recreate().
	 This can be re-enabled in the future when a non-recreate theme-switching
	 solution (like a custom ThemeManager) is implemented.

	<item android:title="@string/dark_mode"
		android:iconTint="@color/textColorPrimary">
		<menu>
			<item
				android:id="@+id/nav_dark_mode_switch"
				android:title=""
				app:actionLayout="@layout/drawer_switch_item"
				app:showAsAction="always" />
		</menu>
	</item>
	-->
</menu>
